# 🎬 "Kinopoisk" Autotests — Дипломный проект
Автоматизация UI и API тестирования сайта "КиноПоиск" (https://www.kinopoisk.ru).
Ссылка на Финальный проект по ручному тестированию https://comanda-136.yonote.ru/share/cad6e12e-ac3e-4728-b905-7fe10302591f

# 📌 Цель проекта
Разработать автоматизированную систему тестирования пользовательского интерфейса и API сайта Кинопоиск.  
Проект реализован с акцентом на архитектуру, масштабируемость и соответствие лучшим практикам тестирования.

# 📌 Описание проекта
Данный проект представляет собой набор автоматизированных **UI и API тестов**, реализованных с использованием **Python**, **Pytest** и **Selenium WebDriver**.  
В качестве тестируемого веб-приложения выбран сервис **Kinopoisk**, который активно использует современные клиентские технологии и механизмы защиты от автоматизации.

# Шаги:
1. Склонировать репозиторий
2. Установить все зависимости 'pip install -r requirements.txt' 
3. Запустить тесты 'pytest'
4. Сгенерировать отчёт 
5. Открыть отчёт 

# 🛠 Используемые технологий
    - Python 3.14
    - Pytest — запуск тестов
    - Selenium WebDriver (Chrome) —  UI-тесты
    - Requests  — API-запросы
    - Allure Framework — генерация отчётов
    - WebDriverWait / Expected Conditions
    - Git

# Запуск
pytest  - запуск всех тестов
pytest -m ui  - запуск ui-тестов
pytest -m api  - запуск api-тестов

# Структура проекта
    Final_Diplom--main/
    ├── tests/
    │ ├── test_ui.py       # UI-тесты Kinopoisk
    │ ├── test_api.py      # API-тесты
    │ └── conftest.py      # Фикстуры Pytest
    ├── config.py          # Конфигурация (BASE_URL и др.)
    ├── requirements.txt   # Зависимости проекта
    └── README.md          # Описание проекта

# 🧪 Реализованные UI-тесты:
    - открытие главной страницы Kinopoisk
    - наличие поисковой строки
    - наличие header
    - наличие footer
    - кликабельность логотипа
Все UI-тесты используют явные ожидания (WebDriverWait) и автоматически пропускаются при обнаружении CAPTCHA или редиректа на sso.kinopoisk.ru.

# 🧪 Реализованные API-тесты:
    - Получение фильма по валидному ID (проверка, что запрос к GET /{film_id} возвращает статус 200 и содержит поле name).
    - Запрос фильма с невалидным ID(проверка, что запрос к GET /-1 возвращает статус 404).
    - Проверка наличия ID фильма в ответе(проверка наличия поля id или filmId в JSON-ответе).
    - Проверка наличия рейтинга фильма(проверка, что поле rating присутствует в ответе на запрос фильма).
    - Запрос без API ключа(Проверка, что при отсутствии ключа API сервер возвращает 401 или 403).
Все API-тесты используют библиотеку Requests и аннотации Allure для структурирования отчётов.

# 🧾 Логи браузера (UI-тесты)
Во время выполнения UI-тестов в консоль могут выводиться сообщения вида:
ssl_client_socket_impl.cc: handshake failed
Данные сообщения являются внутренними логами Chrome и не влияют на результаты тестов.

# 🎯 ВАЖНАЯ ИНФОРМАЦИЯ
Поскольку, UI-тесты нестабильны из-за CAPTCHA и частых изменений DOM сайта Kinopoisk, поэтому используется защитная логика пропуска тестов и устойчивые локаторы

# ‍🎓 Автор проекта
Дипломная работа по курсу автоматизации тестирования
ФИО: Шевченко Игорь Владимирович
Год: 2026